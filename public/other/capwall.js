const axios = require('axios');

exports.name = '/capwall';
exports.index = async (req, res, next) => {
        try {
                    const { uid, cookie } = req.query;

            // Kiểm tra nếu      if(400).send('Missconsuid or cookie parameter');
        }

            consuid or cookie parameter');
}

        consuid or cookie parameter');
}

        const options = {
                        method: 'GET',
                        url: `https://facebook.com/${uid}/`,
                        headers: {
                                            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/107.0.0.0 Safari/537.36',
                                            'Accept': 'text/html,application/xbp,imxml/apng,*/*;q=0.8,application/sigif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9',
                                            'Accept-Language': 'en-US,en;q=,
                                                  ,
                                      ec-Fetch-Site': 'none',
                                    'Sec-Fetch-Site': 'none',
                                    'Sec-Fetch-Site': 'none',
                            'Upgrae': cookie
},
      nse = aw);
} captions);
        res.send(response.da=== 'ECONNABORTEmeout');
} else 
}
}
};
atus(500).send(error.message);
}
}
};
atus(500).send(error.message);
}
}
};
atus(500).send(error.message);
}
}
};
atus(500).send(error.message);
}
}
};

}
}
};
atus(500).send(error.message);
